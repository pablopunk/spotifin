### Rendering

* Keep glass surfaces on `GlassQuality.premium`; the `standard` renderer draws a hairline rim that `SpotifinGlass.settings` cannot subdue.
* Reuse `SpotifinPlayButton` for play and pause; the global `iconButtonTheme` mutes `IconButton.filled` foregrounds.
